	git config --global user.name abhijeet104 
	git config --global user.name
	git condig --global user.email abhijeetae@gmail.com
	git config --global user.email abhijeetae@gmail.com
	git config --global user.email

	git init
	git clone https://github.com/Abhijeet104/MyGithub.git

	git add filename.java
	git commit -m "removeNthFromEnd of LinkedList"
	git push
	//Enter username pwd


-- reset(i.e. delete back) added files by 'git add filename.txt'
	git reset filename.txt

--to see the changes of last 2 commits of a file
git diff filenet.txt

--uncommit the last commit
git reset HEAD~  
